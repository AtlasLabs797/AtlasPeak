<?xml version="1.0" encoding="utf-8"?>
<!-- allowBackup=false a nivel app; estas reglas excluyen explícitamente datos sensibles
     de cloud backup y device transfer. La DB cifrada y las prefs cifradas NO deben salir
     del dispositivo por esta vía (la clave vive en el Keystore y no migra). SEC-007. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
